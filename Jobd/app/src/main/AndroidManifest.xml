<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jobd">
    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".HiringTaskViewActivity"></activity>
        <activity android:name=".HiringOpenActivity" />
        <activity android:name=".TaskViewActivity" />
        <activity android:name=".ShowTaskActivity" />
        <activity android:name=".WorkingHomeActivity" />
        <activity android:name=".HelpActivity" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".NewTaskActivity" />
        <activity
            android:name=".HiringHomeActivity"
            android:label="@string/title_activity_hiring_home" />
        <activity android:name=".ChoiceActivity" />
        <activity android:name=".SigninActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name="com.theartofdev.edmodo.cropper.CropImageActivity"
            android:theme="@style/Base.Theme.AppCompat"/>
    </application>

</manifest>